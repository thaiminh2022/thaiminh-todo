{
  "name": "thaiminh2022todo",
  "version": "0.1.0",
  "description": "A general app",
  "scripts": {
    "setup": "rustup target add wasm32-unknown-unknown && cargo install trunk",
    "dev:serve": "trunk serve",
    "dev:build": "trunk build",
    "prod": "trunk build --release"
  },
  "dependencies": {
    "autosize": "^5.0.1",
    "firebase": "^9.9.2",
    "project": "^0.1.6"
  }
}